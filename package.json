{
  "name": "wt_custom_lang_rev",
  "main": "dist/index.html",
  "scripts": {
    "build": "encore production && sass --no-source-map scss/in:scss/out && tauri build",
    "dev": "encore dev &&  tauri dev",
    "sass_watch": "sass scss/in:scss/out && sass --watch scss/in:scss/out"
  },
  "dependencies": {
    "@symfony/webpack-encore": "^2.1.0",
    "@tauri-apps/api": "^1.0.0-rc.4",
    "node-sass": "^7.0.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.0-rc.9",
    "file-loader": "^6.0.0",
    "ts-loader": "^9.0.0",
    "typescript": "^4.6.4"
  }
}
